// Plugin OpenCode do SDD.
// As skills SDD são descobertas pelo OpenCode a partir do diretório skills/.
// Este módulo existe para satisfazer o campo "main" do package.json e
// permitir registrar comportamento adicional no futuro (hooks de sessão, etc.).

export const sdd = async () => {
  return {};
};

export default sdd;
