# Implementation Log

Registro global de specs concluídas.

## 2026-06-19 — 1: Ask feature flag when creating SDD spec

- **Spec:** `specs/2026-06-1-feature-flag-spec-question/`
- **MR/PR:** _(pending — use `mr-template.md` in spec folder)_
- **Branch:** `feat/1-feature-flag-spec-question`
- **Version:** `0.4.0`
- **Resumo:** Early feature-flag intent in `/sdd-01-new` (`feature_flag` frontmatter); Plan re-evaluation with opt-in to update `AGENTS.md`; Init detects/asks flag conventions; structured Feature flags in bootstrap template and contract.
- **Decisões:** Option A (New + Plan + Init); question for all spec types; no `sdd-feature-flag.md`; project-specific flag mechanics stay in consuming repo `AGENTS.md`.
- **Desvios:** Nenhum bloqueante. `CLAUDE.md` expanded outside T01–T05 scope (prior `/sdd-init` run).
