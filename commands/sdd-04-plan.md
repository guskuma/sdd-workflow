---
description: "SDD 04 — Plan: backlog executável; ondas + subagente planejador/compliance em specs grandes."
---

Use a skill `sdd-04-plan` para a fase de plan da spec ativa (zero-context / bite-sized; `plan_depth` snippets|contracts; Plan em ondas quando ≥2 fases ou ≥8 paths criar; planejador sequencial escreve em `tasks.md`; gate plan-compliance por escopo skeleton|fase-N|cross). Siga-a integralmente. Consulte o `AGENTS.md` do projeto.
