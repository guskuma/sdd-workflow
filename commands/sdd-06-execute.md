---
description: Implementar task/fase SDD com loop de revisão (execute + task review)
---

Use a skill `sdd-06-execute` para implementar a task/fase aprovada e concluir a revisão por task (loop implement → review → receiving-review → fix). Pergunte sobre worktree (opt-in) e invoque `worktrees` se o dev aprovar. Se `tdd: true`, siga a skill `tdd`. Consulte o `AGENTS.md` do projeto.
